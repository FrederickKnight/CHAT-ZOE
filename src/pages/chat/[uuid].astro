---
import ChatLayout from "@layouts/ChatLayout.astro";
import ChatRoom from "@components/chat/chat-room";

const {uuid} = Astro.params;
---

<ChatLayout>
    {uuid &&
        <ChatRoom uuid={uuid} client:load />
    }
</ChatLayout>